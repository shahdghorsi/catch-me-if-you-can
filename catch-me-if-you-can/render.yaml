services:
  - type: web
    name: catch-me-if-you-can
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --worker-class gevent -w 1 --bind 0.0.0.0:$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: SECRET_KEY
        generateValue: true
      - key: FLASK_DEBUG
        value: "false"
      # Resend email settings - configure in Render dashboard
      - key: RESEND_API_KEY
        sync: false
      - key: FROM_EMAIL
        sync: false
